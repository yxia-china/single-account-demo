name: Deploy to Staging

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging   # 关联到staging环境

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Deploy
      run: echo "Deploying to staging..."
      env:
        DB_URL: ${{ secrets.STAGING_DB_URL }}
